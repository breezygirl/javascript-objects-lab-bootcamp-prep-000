var recipes = {
  eggs: 'three',
  onions: 'one',
  peppers: 'greens',
  salt: 'pinch',
  butter: 'tablespoon'
  }
  
  function updateObjectWithKeyAndValue(object, key, value) {
object[key] = value
 
 return object
  }
function updateObjectWithKeyAndValue(object, key, value) {
 var newObject= Object.assign(object, key:value)

 
     
        return object

      }
      